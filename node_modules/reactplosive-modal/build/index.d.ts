import { ReactNode } from "react";
type Props = {
    title?: ReactNode;
    children: ReactNode;
    isVisible: boolean;
    width?: string;
    onClose: (visibility: boolean) => void;
    outsideClickEnabled?: boolean;
};
declare const ReactplosiveModal: ({ title, children, isVisible, onClose, outsideClickEnabled, width, }: Props) => JSX.Element;
export default ReactplosiveModal;

//# sourceMappingURL=index.d.ts.map
